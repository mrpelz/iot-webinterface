/* eslint-disable import/extensions */
import {
  BaseComponent
} from '../../dom.js';

export class Abcde extends BaseComponent {
  constructor(template) {
    super(template);
    console.log(this);
  }
}
