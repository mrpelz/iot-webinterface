/* eslint-disable import/extensions */
import {
  TemplatedComponent,
  getComponent
} from '../../dom.js';

export class Efghi extends TemplatedComponent {
  constructor(template) {
    super(template);
    console.log(this);
  }
}
